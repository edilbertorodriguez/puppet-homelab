---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: Node-specific data
    path: "nodes/%{facts.networking.hostname}.yaml"

  - name: Common data
    path: common.yaml
